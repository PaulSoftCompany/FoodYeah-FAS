spring:
  application:
    name: registry-service
  cloud:
    config:
      uri: http://config-service:8081
      failFast: true
      retry:
        maxAttempts: 20
      username: paulsoftcomp
      password: paulsoftcompany123
